import 'package:flutter/material.dart';

class TabThreeView extends StatelessWidget {
  const TabThreeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Nội dung Tab 3',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
